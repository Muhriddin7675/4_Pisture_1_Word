<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.FindWordKotlin." parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">#151515</item>
        <item name="android:statusBarColor">@color/app_bg</item>
    </style>
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Dialog.Alert">
        <!-- Set your desired background color -->
        <item name="android:background">@color/textColor</item>
        <!-- Set your desired text color -->
        <item name="android:textColor">@color/white</item>
        <!-- Customize other attributes as needed -->
    </style>
    <style name="Base.Theme.FindWordKotlin.a" parent="Base.Theme.FindWordKotlin" />
</resources>